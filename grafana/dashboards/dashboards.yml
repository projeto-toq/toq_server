apiVersion: 1

providers:
  - name: 'toq-server-dashboards'
    orgId: 1
    folder: 'TOQ Server'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
