1. O plano de implementação foi aprovado.
    1.1. Ele contém a arquitetura, o esqueleto de código e as assinaturas de funções necessárias.
    1.2. Prossiga com a implementação completa do código em todos os arquivos listados no plano, preenchendo os esqueletos de função (code skeletons) com a lógica de negócio, de repositório e de handler, estritamente de acordo com as regras de arquitetura do manual (observabilidade, erros, transações, etc.) já detalhadas no plano.
    1.3. Foque apenas na geração do código final e não na reanálise do problema.
2. Garanta que ao final não existam errors/warnings no código com execução de make lint.
3. Eventuais deleçãoes de arquivo devem ser feitas pelo comando rm na CLI.