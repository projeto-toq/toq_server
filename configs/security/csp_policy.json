{
    "version": 1,
    "description": "Baseline CSP for the TOQ web client",
    "directives": {
        "default-src": "'self'",
        "img-src": "'self' https://cdn.toq.dev",
        "script-src": "'self' https://cdn.toq.dev",
        "style-src": "'self' 'unsafe-inline' https://fonts.googleapis.com",
        "font-src": "'self' https://fonts.gstatic.com"
    },
    "notes": [
        "Inclua apenas diretivas necessárias.",
        "Use espaços para separar valores múltiplos em cada diretiva."
    ]
}